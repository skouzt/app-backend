# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.8.2
python-dotenv==1.0.0
websockets==12.0

# Pipecat AI (Daily only, no OpenAI dependency)
pipecat-ai[daily]==0.0.45

# HTTP Client
httpx==0.25.2

# Whisper STT (Open Source)
openai-whisper==20231117
# OR use Faster Whisper (4x faster - RECOMMENDED)
# faster-whisper==1.0.0

# Google Gemini LLM (Free tier available!)
google-generativeai>=0.3.0

# Kokoro TTS v1.0 82M (Open Source)
# Install from source if not on PyPI:
# pip install git+https://github.com/hexgrad/Kokoro-82M.git

# Audio Processing (numpy managed by pipecat-ai ~= 1.26.4)
soundfile==0.12.1

# PyTorch (required by Whisper and Kokoro)
torch>=2.0.0
# For GPU support (optional):
# torch==2.1.0+cu118 --extra-index-url https://download.pytorch.org/whl/cu118

# Additional dependencies
python-multipart==0.0.6
aiofiles==23.2.1

# Optional: Database support (uncomment if needed)
# sqlalchemy==2.0.23
# psycopg2-binary==2.9.9

# Development dependencies (optional)
# pytest==7.4.3
# black==23.12.0